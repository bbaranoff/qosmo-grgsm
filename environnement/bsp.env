# =============================================================================
#  environnement/bsp.env — BSP, DMA, DARAM, feed I-Q, horloge TDMA
# =============================================================================
#  Les quatre valeurs ci-dessous sont MESUREES, pas devinees. Ne pas les changer
#  sans refaire la mesure correspondante.
# -----------------------------------------------------------------------------

# Adresse ou le BSP ecrit le burst dans la DARAM du DSP. 0 = mode decouverte.
# ⚠️ [2026-08-22] 0x4c00 etait FAUX et DESTRUCTEUR — corrige en 0x2a00.
# 0x4c00 est un BLOC DE CONTROLE du DSP, pas un tampon : 104 de ses cellules sont
# adressees en ABSOLU par 247 sites de PROM0, et `0xd95f ld *(0x4c0c),B ; cala B`
# y prend un POINTEUR DE HANDLER que le DSP APPELLE. Ecrire le burst dessus
# faisait sauter le DSP a une adresse tiree d'un echantillon I/Q.
# 0x2a00 : 0 reference absolue, 51 `STM #0x2a00,ARx` = region PARCOURUE = tampon.
# Canari (CALYPSO_BSP_INJECT_CANARY=1) : @0x4c00 -> 4 lectures d'UN mot ;
# @0x2a00 -> 100 lectures a 100 adresses SEQUENTIELLES, PC=0x8166/0x8175, dont la
# boucle `stm #0x2a00,AR2 ; rptbd ; mpyr *AR2,A ; sth *AR2+,A` (58 tours).
# L'ancienne justification citait PC=0x9fb5 — JAMAIS atteint (comme 0x93a5).
# Remettre 0x4c00 pour comparer, en sachant que ca corrompt le DSP.
: "${CALYPSO_BSP_DARAM_ADDR:=0x2a00}"

# Longueur de la fenetre d ecriture, en mots de 16 bits.
# 296 = 148 echantillons complexes = un burst apres decimation.
: "${CALYPSO_BSP_DARAM_LEN:=296}"

# Decimation du flux I-Q.
# /!\ 1 est une REGRESSION MESUREE : le feed arrive a 4 SPS et le correlateur
# voit dphi=+0.25x pi/2 au lieu de +1.00. Avec 4, corr_iq.py --src bursts
# affiche « FCCH @1SPS PROPRE ».
: "${CALYPSO_BSP_IQ_DECIM:=4}"

# Nombre d instructions DSP par tranche d execution (cadence).
: "${CALYPSO_DSP_BUDGET:=8000}"

# Adresse d ecoute du listener TRXDv0.
: "${CALYPSO_BSP_BIND_ADDR:=0.0.0.0}"

# -----------------------------------------------------------------------------
#  Inventaire complet du domaine, par categorie
# -----------------------------------------------------------------------------
# --- Parametres legitimes (17) ----------------------------------------
#  Reglables. Le materiel reel a un equivalent.

#   defaut : code 0.0.0.0 (bsp.c:907)
: "${CALYPSO_BSP_BIND_ADDR:=}"

#   defaut : unset (bsp.c:900)
: "${CALYPSO_BSP_BIND_LOOPBACK:=}"

#   defaut : code 0x2a00 (bsp.c:823) ; run.sh:1303 :-0x2a00 ; **live 0x4c00**
: "${CALYPSO_BSP_DARAM_ADDR:=}"

#   defaut : code 296 (bsp.c:824) ; live 296
: "${CALYPSO_BSP_DARAM_LEN:=}"

#   defaut : code 4 (bsp.c:591) ; **live =4**
: "${CALYPSO_BSP_IQ_DECIM:=}"

#   defaut : ON-sauf-0 (bsp.c:570-577) ; run.sh:1342 :-1 ; **live =1**
: "${CALYPSO_BSP_IQ_PASSTHROUGH:=}"

#   defaut : code BSP_TRXD_PORT=6702 (bsp.c:58, 913)
: "${CALYPSO_BSP_PORT:=}"

#   defaut : unset (bsp.c:868)
: "${CALYPSO_BSP_REPLAY_FILE:=}"

#   defaut : ON-sauf-0 (trx.c:1239) ; **live ON** (log [cpu-idle] governor ON ...
: "${CALYPSO_CPU_IDLE:=}"

#   defaut : code 0x00823000 (trx.c:1240)
: "${CALYPSO_IDLE_PC_LO:=}"

#   defaut : code 0x00826000 (trx.c:1241)
: "${CALYPSO_IDLE_PC_HI:=}"

#   defaut : code 2500 (dsp_shunt.c:1794, 1814)
: "${CALYPSO_IQ_CFILE_SPF:=}"

#   defaut : code 127.0.0.1 (bsp.c:410-419)
: "${CALYPSO_IQ_TEE_HOST:=}"

#   defaut : code 6703 (bsp.c:405-406) ; run.sh:1224 :=6703 ; **live =6703**
: "${CALYPSO_IQ_TEE_PORT:=}"

#   defaut : code D_RACH_DEFAULT_OFFSET=0x023A (bsp.c:1741) ; run.sh:1334 :- ;...
: "${CALYPSO_NDB_D_RACH_OFFSET:=}"

#   defaut : code WALL_TDMA_NS=4615384 (trx.c:1036, 1050-1051) ; **absente du ...
: "${CALYPSO_TDMA_NS:=}"

#   defaut : calypso.env:23 :=1, run.sh:1312 :-1 ; **live =1** (log REALTIME (...
: "${CALYPSO_TDMA_REALTIME:=}"


# --- Sondes et traces (20) --------------------------------------------
#  Sans effet sur l emulation. Activez librement.

#   defaut : calypso.env:137 :=1, wire.env:35 :=1 ; **live =1**
# : "${CALYPSO_BSP_FN_PROBE:=1}"

#   defaut : code 0 (bsp.c:847)
# : "${CALYPSO_BSP_INJECT_CANARY:=1}"

#   defaut : code 0 (bsp.c:1234)
# : "${CALYPSO_BSP_IQ_SHIFT:=1}"

#   defaut : unset (c54x.c:15666) ; **live =1**
# : "${CALYPSO_DARAM_DUMP:=1}"

#   defaut : code 0x9ac0 (c54x.c:15665, 15671)
# : "${CALYPSO_DARAM_DUMP_PC:=1}"

#   defaut : code 200 (c54x.c:15665, 15673)
# : "${CALYPSO_DARAM_DUMP_MAX:=1}"

#   defaut : code 0 (c54x.c:15685)
# : "${CALYPSO_DARAM_DUMP_ANYMODE:=1}"

#   defaut : unset (c54x.c:1824, 3002)
# : "${CALYPSO_DMAWATCH:=1}"

#   defaut : unset ; **absente du run**
# : "${CALYPSO_IQDUMP:=1}"

#   defaut : unset (bsp.c:1031)
# : "${CALYPSO_IQDUMP_FCCH:=1}"

#   defaut : unset (c54x.c:1868, 3075)
# : "${CALYPSO_MEM_WATCH_2B80:=1}"

#   defaut : unset (c54x.c:1852, 15492)
# : "${CALYPSO_SLOTSRC:=1}"

#   defaut : code 0 (c54x.c:1931-1933)
# : "${CALYPSO_WATCH_RD_ADDR:=1}"

#   defaut : code 0 (c54x.c:3701-3703)
# : "${CALYPSO_WATCH_WR_ADDR:=1}"

#   defaut : unset (c54x.c:2885) ; **live =1**
# : "${CALYPSO_WMAP:=1}"

#   defaut : code 0x2c00 (c54x.c:2888) ; **live 0x2a00**
# : "${CALYPSO_WMAP_LO:=1}"

#   defaut : code 0x2c1f (c54x.c:2888) ; **live 0x2a1f**
# : "${CALYPSO_WMAP_HI:=1}"

#   defaut : code 0xffff (c54x.c:2891)
# : "${CALYPSO_WMAP_LO2:=1}"

#   defaut : code 0x0000 (c54x.c:2891)
# : "${CALYPSO_WMAP_HI2:=1}"

#   defaut : unset ; **absente du run**
# : "${CALYPSO_WZWRITE:=1}"


# --- BEQUILLES (10) ---------------------------------------------------
#  Contournent une branche non implementee. Ce sont des dettes, pas des options.
#  Chacune est annotee a son site de lecture : grep -rn "@BEQUILLE" hw/arm/calypso/

#   defaut : calypso_wire.env:46 :=1 ; **live =1**
# : "${CALYPSO_BSP_DARAM_FORCE:=1}"

#   defaut : calypso.env:225 :=1 (bloc WIRE) et wire.env:27 :=1 — **inactive h...
# : "${CALYPSO_BSP_DIRECT_BRINT0:=1}"

#   defaut : calypso.env:141 :=1, native.env:18 :=1 ; **live =1**
# : "${CALYPSO_BSP_DIRECT_FEED:=1}"

#   defaut : code 0x8d00 (bsp.c:1148)
# : "${CALYPSO_BSP_DISPATCH_FB_TGT:=1}"

#   defaut : unset (bsp.c:1165)
# : "${CALYPSO_BSP_DISPATCH_NOIMR:=1}"

#   defaut : wire.env:54 **unset explicite**
# : "${CALYPSO_BSP_DISPATCH_ONESHOT:=1}"

#   defaut : code 0 (trx.c:157-167) ; **aucun .env, absente du run**
# : "${CALYPSO_DL_FN_OFFSET:=1}"

#   defaut : calypso.env:145 :=0 ; **live =0**
# : "${CALYPSO_FB_IQ_OWNS:=1}"

#   defaut : **code 1 si la variable est ABSENTE** (bsp.c:1116)
# : "${CALYPSO_POKE_TASK_MD:=1}"

#   defaut : unset → -1 (bsp.c:1766-1786) ; run.sh:1335 :- ; **live vide**
# : "${CALYPSO_RACH_FORCE_BSIC:=1}"


# --- MORTES — candidates au retrait (1) ------------------------------
#  Plus lues nulle part, ou lues dans du code non lie.

#   defaut : code 0 (bsp.c:837)
# : "${CALYPSO_BSP_BYPASS_BDLENA:=1}"


# --- Non classees (4) ------------------------------------------------
#  A trancher : relire l effet dans le code execute.

#   defaut : wire.env:28 :=1 ; absente du run vivant
# : "${CALYPSO_BSP_DISPATCH_FB:=1}"

#   defaut : code 0 (bsp.c:1123)
# : "${CALYPSO_POKE_DISPATCH:=1}"

#   defaut : unset ; **absente du run**
# : "${CALYPSO_RX_FBFLAGS:=1}"

#   defaut : wire.env:61 :=1 ; **absente du run vivant**
# : "${CALYPSO_TPU_RX_WIRE:=1}"


#   [2026-08-04] FEED-FP : empreinte FNV-1a du burst COMPLET a l entree UDP
#   (bsp_trxd_readable) et a la sortie vers le RIF (c54x_bsp_load). Sonde
#   LECTURE SEULE, plafonnee (20 premieres puis 1/500). Sert a encadrer le gel
#   du burst : IN varie + OUT constant = gel entre les deux.
#   defaut : code 0
# : "${CALYPSO_BSP_FINGERPRINT:=1}"

#   [2026-08-04] FEED-DST : quel tampon le demod LIT-il ? Compte les lectures
#   DSP dans A=0x0cce..0x0df5 (destination du DMA) et B=0x4c00..0x4d27
#   (correlateur). A>0 B=0 = le demod lit le DMA, chercher en aval. A=0 B>0 =
#   il lit un tampon non alimente. Sonde LECTURE SEULE, plafonnee, s annonce
#   au demarrage pour qu un silence soit un RESULTAT et pas un doute.
#   defaut : code 0
: "${CALYPSO_FEED_DST:=1}"
