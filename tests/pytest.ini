[pytest]
testpaths = .
python_files = test_*.py
# "Mode complet au max" : verbose + short tb + summary all + strict markers.
# Override via CLI flags si besoin (ex: pytest -m inject_frames).
addopts = -v -ra --tb=short --strict-markers --durations=0
